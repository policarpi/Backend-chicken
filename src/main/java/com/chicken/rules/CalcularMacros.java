package com.chicken.rules;

import static java.lang.Float.parseFloat;
import static java.lang.Float.valueOf;

public class CalcularMacros {
/*
    public void taxaMetabolica(Integer peso, Integer Idade, Integer Altura) {

        static String tipo;

        double vresultado = 0;

        if (tipo == "M") {
            vresultado = (double) (66 + (13.7 * peso) + (5 * Altura) - (6.8 * Idade));
        } else {
            vresultado = (double) (655 + (9.6 * peso) + (1.7 * Altura) - (4.7 * Idade));
        }

    }

    public double taxaIntencidade( Integer pesoCm, Integer idadeCm, Integer alturaCm, String tipoS ){

        double total = taxaMetabolica(taxaMetabolica(pesoCm,idadeCm,alturaCm)) + taxaMetabolica() * 0.25;


            var total = vresultado + (vresultado * 0.25);

            total = vresultado + (vresultado * 0.35);
            vmoderada = parseFloat(total);
            form.moderada.value = vmoderada.toFixed(2);

            total = vresultado + (vresultado * 0.45);
            vintensa = parseFloat(total);
            form.intensa.value = vintensa.toFixed(2);
        } else {
            vresultado = (double) (655 + (9.6 * peso) + (1.7 * Altura) - (4.7 * Idade));
        }
        return vresultado;
        return taxaMetabolica();
    }
*/
}
